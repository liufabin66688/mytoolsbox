// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("say_hello", "for say_hello support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("say_hello", "enable say_hello support", "no");

if (PHP_SAY_HELLO != "no") {
	EXTENSION("say_hello", "say_hello.c");
}

